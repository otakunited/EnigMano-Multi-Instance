name: "⚡ EnigMano Win11 + Chrome Remote Desktop (Fixed CRD Install)"

on:
  workflow_dispatch:

jobs:
  deploy-enigmano:
    runs-on: windows-latest

    steps:
      - name: 🌐 Install Google Chrome
        shell: pwsh
        run: |
          $chromeInstaller = "$env:TEMP\chrome_installer.exe"
          Invoke-WebRequest "https://dl.google.com/chrome/install/latest/chrome_installer.exe" -OutFile $chromeInstaller
          Start-Process $chromeInstaller -ArgumentList "/silent /install" -Wait

      - name: 🌐 Install Chrome Remote Desktop Host (fixed URL)
        shell: pwsh
        run: |
          $crdInstaller = "$env:TEMP\chromeremotedesktophost.msi"
          Invoke-WebRequest "https://dl.google.com/edgedl/chrome-remote-desktop/chromeremotedesktophost.msi" -OutFile $crdInstaller
          Start-Process msiexec.exe -ArgumentList "/i `"$crdInstaller`" /qn" -Wait

      - name: ⚙️ Configure Chrome Remote Desktop
        shell: pwsh
        run: |
          $exe = "${Env:ProgramFiles(x86)}\Google\Chrome Remote Desktop\CurrentVersion\remoting_start_host.exe"
          if (-not (Test-Path $exe)) {
            Write-Error "❌ CRD executable not found at $exe"
            exit 1
          }

          # 🔑 Mets ici ton vrai code CRD Headless
          $code = "4/0AVMBsJhIqShthYalxeb90iucACB0oQxiWKlYOh5aYgT1701tiAriiPjIbwH1l5Y9l8e-wA"

          & "$exe" `
            --code=$code `
            --redirect-url="https://remotedesktop.google.com/_/oauthredirect" `
            --name="EnigMano-Win11" `
            --pin=123456

          Write-Host "✅ CRD host registered. PIN: 123456"

      - name: ♾️ Keep session alive
        shell: pwsh
        run: |
          Write-Host "🔄 Keeping session alive..."
          Start-Sleep -Seconds 21600
